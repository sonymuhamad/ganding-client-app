import { createStyles } from "@mantine/core";



export const DeliveryReportStyle = createStyles((theme) => ({

    image: {
        maxWidth: '30%',
        width: 90,
        marginLeft: 50
    }
}))